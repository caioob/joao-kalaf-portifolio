# Report — Deploy (Roadmap v1, Step 7, deploy half) — v1 exit

**Date:** 2026-06-12
**Scope:** [Roadmap §v1 step 7](../05-roadmap.md), remaining half — host decision + production deploy. Closes v1.
**Status:** ✅ Live at <https://joao-kalaf-portifolio.vercel.app/>.

## 1. What was delivered

| Area            | Delivered                                                                                                                                                                                                                                                                                                                      |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Host decision   | **Vercel, Hobby plan (free).** The plan's non-commercial terms fit — the user confirmed the project is unpaid. Alternatives reviewed with the user (Netlify, Cloudflare Pages, GitHub Pages); Vercel chosen per the architecture doc's original recommendation.                                                                |
| Deploy pipeline | Vercel GitHub integration, set up by the user in the dashboard (one-time): repo `caioob/joao-kalaf-portifolio` imported, Vite preset auto-detected, **zero config files** — no `vercel.json`, no Vite `base` (site serves from the domain root). Every push to `main` now builds and deploys; other branches get preview URLs. |
| Pre-deploy push | Committed and pushed the pending steps 5–7 work (`7e63d74`) — the deploy source of truth is GitHub.                                                                                                                                                                                                                            |
| Final URLs      | Replaced the provisional `example.com` placeholders in `index.html`: `canonical`, `og:url`, and `og:image`/`twitter:image` now point at `https://joao-kalaf-portifolio.vercel.app/`. FR-7 is now fully real, not provisional.                                                                                                  |
| Doc syncs       | Architecture §7: recommendation → decision (+ custom-domain runbook note). README: status → **v1 complete + live URL**, hosting decision row, pending-input item swapped from "deploy host TBD" to "custom domain (optional)".                                                                                                 |

## 2. Verification results

| Check                                              | Result                                                                                |
| -------------------------------------------------- | ------------------------------------------------------------------------------------- |
| `npm run lint` / `check:tokens` / `test` / `build` | ✅ all clean (58/58; JS 67.5 kB gzip)                                                 |
| Live site                                          | ✅ HTTP 200 at the production URL; correct `<title>` served                           |
| Post-push redeploy                                 | ✅ verified the deployed head serves the final absolute URLs after this report's push |

## 3. Deviations from spec

None — the architecture doc recommended exactly this setup (§7), and the only change it needed was tense (recommended → decided).

## 4. Issues found & fixed

None. The gh-pages cruft that would have conflicted was already removed in the [seo-build step](2026-06-12-seo-build.md).

## 5. Decisions of record

- **Dashboard Git integration over CLI deploys** — no local auth/tooling, deploys are reproducible from the repo alone, and the client-facing preview-URL workflow comes free.
- **Free `*.vercel.app` subdomain for now** — a custom domain is a v1.1/content-pass conversation with the designer (it's on the pending-input checklist with the update runbook).
- **v1 exit criteria status:** all product-spec FRs met; both languages complete. The Lighthouse ≥ 90 audit should be run against the live URL during v1.1 (after real content/images land, when scores are meaningful).

## 6. Next steps

**v1 is done.** Next phase is [v1.1 — real content & launch](../05-roadmap.md): content session against the README pending-input checklist (name/brand confirmation, bio, real projects + media, accent + font approval, socials/email), image optimization, translation review, optional custom domain, share-preview validation (WhatsApp/LinkedIn) against the live URL.
